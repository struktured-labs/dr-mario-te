# PRE-REG (2026-09-24 AT LAUNCH, Claude solo): opponent-aware chain dose vs a modelled human
Owner: "do both" (freeze root-cause + make the chain attack read the human). Base brain = fw_winner
(winner leaf + chain180 + strand20 = θ400 recipe; the recommended couch brain). Policies choose the
chain dose PER PILL from (own viruses o, human viruses p) — both visible to the cart on silicon:
  fw180 (baseline) · fw360 (blind, more chain) · press: 360 if o-p>=3 or p<=8 else 180 ·
  cruise: 90 if p-o>=5 else 180 · press_cruise: both rules.
Live race (vs_race_adaptive.play_live): human pace T_L0 ~ lognormal(M, 0.15), damage 2.65 s/tile
(fitted), linear virus countdown (DECLARED), Poisson volleys lam=6/min (in-game expert rate), ROM-true
mechanics, cart clock. Identity: fw180 live == fw_winner post-hoc on 3/3 seeds.
Cells: M=177 (Hartford expert median) and M=220 (slower human). n=300 seeds 36734.. step 2 (declared
reuse; paired with all VS-race/gate-b arms). PRIMARY: paired win-rate difference vs fw180 at M=177,
seed bootstrap. HOLDOUT (Hetzner, registered now): M=177, all 5 policies, seeds 40134.. step 2, n=200.
A policy is "better" only if its paired CI excludes 0 in BOTH blocks AND its loss_kill count is not
higher than fw180's (survival must not be bought back). Silicon path if one wins: firmware reads the
dose from a mailbox byte the cart writes (one pinned-seed rebuild).
