#!/usr/bin/env python3
"""REACHABILITY-AWARE ROOT (STEER1 arm 5): `_choose_d3_chain_s_masked` = a MECHANICAL COPY of
cascade_stranded_x._choose_d3_chain_s (generated by textual transform: explicit numba signature -> inferred,
one added `allowed` int8[32] argument, one added root `continue`). Root actions with allowed[a] == 0 are
skipped. With an all-ones mask it must be action-identical to the stranded decider (selfcheck()).
ReachAwareDecider builds the mask with steer_model: a root action is allowed iff the couch driver, at the
median silicon answer latency, actually lands the capsule on it.
"""
from __future__ import annotations
import numpy as np
from numba import njit, int8, int32, int64, float64
from cascade_stranded_x import *                     # noqa  (helpers the copy relies on)
from cascade_stranded_x import _g_stranded47, StrandedChainD3Decider, _choose_d3_chain_s
from cascade_chain_x import (_leaf_chain, _imm_chain, _base_scan, _expected_third_chain, NBASE, NT)
from fast_sim_x import NCELL, _virus_count, _stable_desc
from fast_rtl_x import _VAR_OF_O4, _WIN_SHIP, _g_excav_ship, _g_hang_ship, _W_EXCAV_SHIP, _W_HANG_SHIP
from cascade_link_x import board_flat
import steer_model as SM


@njit(cache=True)
def _choose_d3_chain_s_masked(pcol, pvir, plnk, ca, cb, na, nb, topk2, w_excav, w_hang,
                              w, fl, maxpass, w_chain, ws, allowed):
    """`cascade_link_x._choose_d3_linked` with the chain bonus folded into every imm.
    At w_chain=0 this must return the identical action."""
    c1 = np.empty(NCELL, dtype=int8); v1 = np.empty(NCELL, dtype=int8)
    l1 = np.empty(NCELL, dtype=int8)
    b2col = np.empty((32, NCELL), dtype=int8); b2vir = np.empty((32, NCELL), dtype=int8)
    b2lnk = np.empty((32, NCELL), dtype=int8)
    keys2 = np.empty(32, dtype=float64); imms2 = np.empty(32, dtype=int64)
    order2 = np.empty(32, dtype=int32)
    s2c = np.empty(NCELL, dtype=int8); s2v = np.empty(NCELL, dtype=int8)
    s2l = np.empty(NCELL, dtype=int8)
    e2c = np.empty(NCELL, dtype=int8); e2v = np.empty(NCELL, dtype=int8)
    e2l = np.empty(NCELL, dtype=int8)
    tc = np.empty(NCELL, dtype=int8); tv = np.empty(NCELL, dtype=int8)
    tl = np.empty(NCELL, dtype=int8); mask = np.empty(NCELL, dtype=int8)
    base1 = np.empty(NBASE, dtype=int64); base2 = np.empty(NBASE, dtype=int64)
    base3 = np.empty(NBASE, dtype=int64); terms = np.empty(NT, dtype=int64)
    _base_scan(pcol, pvir, fl, base1)
    best_val = int64(0); best_act = -1; have = False
    for o4 in range(4):
        var = _VAR_OF_O4[o4]
        for cl in range(8):
            if allowed[var * 8 + cl] == 0:
                continue
            ok, nv, cells, leaf1, ch1 = _leaf_chain(pcol, pvir, plnk, base1, var, cl,
                                                    ca, cb, w, fl, c1, v1, l1, mask,
                                                    terms, maxpass, True)
            if ok == 0:
                continue
            imm1 = _imm_chain(nv, cells, ch1, w, w_chain)
            if _virus_count(v1) == 0:
                val = imm1 + int64(_WIN_SHIP)
            else:
                _base_scan(c1, v1, fl, base2)
                m2 = 0
                for o42 in range(4):
                    var2 = _VAR_OF_O4[o42]
                    for cl2 in range(8):
                        ok2, nv2, cells2, lv2, ch2 = _leaf_chain(
                            c1, v1, l1, base2, var2, cl2, na, nb, w, fl,
                            s2c, s2v, s2l, mask, terms, maxpass, True)
                        if ok2 == 0:
                            continue
                        imm2 = _imm_chain(nv2, cells2, ch2, w, w_chain)
                        keys2[m2] = float64(imm2 + lv2)
                        imms2[m2] = imm2
                        for i in range(NCELL):
                            b2col[m2, i] = s2c[i]; b2vir[m2, i] = s2v[i]; b2lnk[m2, i] = s2l[i]
                        m2 += 1
                if m2 == 0:
                    val = imm1 + leaf1
                else:
                    _stable_desc(keys2, m2, order2)
                    kk2 = m2 if topk2 <= 0 or topk2 > m2 else topk2
                    best2 = int64(0); have2 = False
                    for s2 in range(kk2):
                        k2 = order2[s2]
                        for i in range(NCELL):
                            e2c[i] = b2col[k2, i]; e2v[i] = b2vir[k2, i]; e2l[i] = b2lnk[k2, i]
                        if _virus_count(e2v) == 0:
                            v2 = imms2[k2] + int64(_WIN_SHIP)
                        else:
                            v2 = imms2[k2] + _expected_third_chain(
                                e2c, e2v, e2l, w, fl, base3, terms, tc, tv, tl, mask,
                                maxpass, w_chain)
                        if not have2 or v2 > best2:
                            best2 = v2; have2 = True
                    val = imm1 + leaf1 + ((best2 - leaf1) >> int64(1))
                val += w_excav * _g_excav_ship(c1, v1) + w_hang * _g_hang_ship(c1, v1)
            val -= ws * _g_stranded47(c1, v1)
            if not have or val > best_val:
                best_val = val; best_act = var * 8 + cl; have = True
    return best_act



class ReachAwareDecider(StrandedChainD3Decider):
    def __init__(self, weights, flags, topk2=8, maxpass=0, w_chain=540, ws=20, t_act=None, proph="throat",
                 steer_kw=None):
        super().__init__(weights, flags, topk2=topk2, maxpass=maxpass, w_chain=w_chain, ws=ws)
        self.steer_kw = dict(steer_kw or {})          # STEER3: the mask simulates the same steering (e.g. tap period)
        lat = SM.latency_samples()
        self.t_act = int(t_act) if t_act is not None else lat[len(lat) // 2]
        self.proph = proph
        self.masked = 0; self.calls = 0

    def mask(self, board, k):
        color = board.color.tolist()
        allowed = np.zeros(32, dtype=np.int8)
        for a in range(32):
            var, col = a // 8, a % 8
            if SM.straight_cells(color, var, col) is None:
                continue
            st = SM.Steer(proph=self.proph, seed=0, **self.steer_kw)
            r = st.execute(color, a, k, t_act=self.t_act, phase=1)
            allowed[a] = 1 if r["exact"] else 0
        if not allowed.any():
            allowed[:] = 1
        return allowed

    def choose(self, board, cur, nxt, k=0):
        col, vir = board_flat(board)
        lnk = np.ascontiguousarray(board.link, dtype=np.int8).reshape(-1)
        allowed = self.mask(board, k)
        self.calls += 1
        a = _choose_d3_chain_s_masked(col, vir, lnk, cur.a, cur.b, nxt.a, nxt.b, self.topk2,
                                      self.w_excav, self.w_hang, self.w, self.fl,
                                      self.maxpass, self.w_chain, self.ws, allowed)
        return None if a < 0 else int(a)


class ReachFwDecider(ReachAwareDecider):
    """STEER2 post-hoc: the mask is the CLOSED-FORM FIRMWARE RULE (reach_fw.reach_mask_fw, strict: the capsule
    lands exactly on the straight-drop cells), i.e. what the reach-root firmware would compute."""

    def mask(self, board, k):
        import reach_fw as RF
        return np.asarray(RF.reach_mask_fw(board.color.tolist(), SM.table_threshold(k)), dtype=np.int8)


def selfcheck(n_games=2, seed0=36734):
    """All-ones mask == the stranded decider on every board of real fw540 gate-(b) games."""
    import gate_b as G, bursty_model as BM, vs_race as V, fast_rtl_x as FX
    from drmario.faithful_game import Pill
    w, fl = FX.variant("winner")
    ref = StrandedChainD3Decider(w, fl, topk2=8, maxpass=0, w_chain=540, ws=20)
    m = BM.fit_struktured_20260804(); boards = []
    base = V._decider("fw540")
    def choose(env, col, vir, ctx):
        boards.append((env.board.clone(), Pill(int(env.cur.a), int(env.cur.b)), Pill(int(env.nxt.a), int(env.nxt.b))))
        return base(env, col, vir, ctx)
    for s in range(n_games):
        G.play(seed0 + 2 * s, None, m, choose=choose)
    ones = np.ones(32, dtype=np.int8); same = 0
    for b, c, nx in boards:
        col, vir = board_flat(b); lnk = np.ascontiguousarray(b.link, dtype=np.int8).reshape(-1)
        a1 = ref.choose(b, c, nx)
        a2 = _choose_d3_chain_s_masked(col, vir, lnk, c.a, c.b, nx.a, nx.b, 8, ref.w_excav, ref.w_hang,
                                       ref.w, ref.fl, 0, 540, 20, ones)
        same += int(a1 == (None if a2 < 0 else int(a2)))
    print(f"reach selfcheck: {same}/{len(boards)} identical with an all-ones mask")
    return same == len(boards)


if __name__ == "__main__":
    import sys
    sys.exit(0 if selfcheck() else 1)
