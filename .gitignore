# Virtual environment
.venv/

# Python cache
__pycache__/
*.pyc

# ROMs (don't commit copyrighted content)
*.nes

# Emulator save states and configs
*.sav
*.srm
*.state

# Scratch / scratchpad (ad-hoc debug + cross-check harnesses)
tmp/
log/
setenv.sh

# transient emulator/run logs
*.log
mesen*.log

# expansion test ROMs
drmario_exp.nes
/tmp/
drmario_v28cs_exp.nes
drmario_banktest.nes
drmario_cart.nes

# recorded gameplay clips (large binaries)
*.mp4
# agent scratch
.letta/

# scratch manifests (leading underscore) — not shipped builds
roms/manifests/_*.json
release/kit_out/

# task #49 fingerprint/candidate-build scratch carts (built artifacts, *.nes already ignored
# above, but the whole dir is scratch and shouldn't be scanned/committed as source)
tmp_carts/
